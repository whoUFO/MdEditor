# Electron Markdown Editor 项目Code Wiki

**开发者**: 胡宇峰  
**联系邮箱**: hyf2k@163.com

## 一、项目概述

### 1.1 项目简介

本项目是一个基于 Electron + React 技术栈开发的现代化 Markdown 编辑器，采用 Turborepo + pnpm 进行 Monorepo 管理。项目代号为"Electron Markdown Editor"，当前版本为 v0.1.0-alpha，已完成全部 76 个迭代任务。

### 1.2 核心价值

本编辑器提供了完整的 Markdown 编辑体验，涵盖从基础的文本编辑到高级的富文本渲染功能。编辑器支持所见即所得的双栏实时预览，采用 300ms 自适应防抖技术确保流畅的用户体验。同时，项目实现了完善的文件管理功能，支持目录树浏览、文件打开与保存，并具备自动编码检测能力，能够处理 GBK、Shift-JIS 等多种字符编码。

### 1.3 版本信息

当前版本标识为 v0.1.0-alpha，遵循语义化版本规范。项目开发分为四个主要阶段：M0 阶段完成 Monorepo 基础设施搭建；P1 阶段完成 Electron + React 基础框架；P2 阶段实现核心编辑功能闭环；P3 阶段完成增强功能开发；P4 阶段完成打磨与发布工作。

## 二、技术架构

### 2.1 技术栈总览

本项目采用现代化的前端技术栈构建桌面应用，具体技术选型如下表所示：

| 技术层次 | 技术选型 | 版本号 | 说明 |
|---------|---------|--------|------|
| 桌面框架 | Electron | 41 | 跨平台桌面应用框架 |
| 前端框架 | React | 19 | UI渲染库 |
| 编程语言 | TypeScript | 6 | 支持 strict 模式 |
| 构建工具 | Vite | 8 | 快速打包与热更新 |
| 任务管理 | Turborepo | 2.9 | Monorepo 构建编排 |
| 包管理器 | pnpm | 9.15 | 高效磁盘空间管理 |
| 编辑器内核 | CodeMirror | 6 | 代码编辑器组件 |
| Markdown处理 | unified | - | remark + rehype 生态 |
| 数学公式 | KaTeX | - | LaTeX 数学公式渲染 |
| 图表渲染 | Mermaid | - | 流程图与时序图 |
| 代码高亮 | highlight.js | - | 多语言代码着色 |
| 状态管理 | Zustand | - | 轻量级状态管理库 |
| 安全防护 | CSP + 沙箱 | - | 内容安全策略与隔离 |

### 2.2 架构分层设计

本项目采用经典的三层架构模式，通过 Electron 的主进程、Preload 桥接层和渲染进程实现功能分离。这种设计确保了主进程与渲染进程之间的安全通信，同时通过 Preload 层提供了受控的 API 暴露机制。

主进程层负责处理系统级操作，包括窗口管理、文件系统访问、系统对话框调用以及应用生命周期管理。主进程运行在 Node.js 环境中，具有完整的系统访问权限，因此必须谨慎处理安全相关操作。

Preload 桥接层作为主进程与渲染进程之间的安全通道，采用 contextIsolation 技术实现进程隔离。Preload 脚本在渲染进程加载前执行，通过暴露特定的 API 接口供渲染进程调用，从而避免直接暴露 Node.js 功能到前端页面。

渲染进程层运行 React 应用，负责构建用户界面和处理用户交互。该层运行在 Chromium 沙箱环境中，对系统资源的访问受到严格限制，所有文件操作必须通过 IPC 机制委托给主进程完成。

## 三、项目结构

### 3.1 目录组织

本项目采用 Monorepo 结构组织代码，根目录包含 apps 和 packages 两个主要工作区，具体目录结构如下：

```
├── apps/                              # 应用工作区
│   └── markdown-editor/               # Electron 桌面应用主包
│       ├── src/                       # 源代码目录
│       │   ├── main/                 # Electron 主进程代码
│       │   ├── preload/              # Preload 桥接层代码
│       │   └── renderer/              # React 渲染进程代码
│       ├── docs/reports/             # 审查与测试报告文档
│       └── package.json              # 应用包配置
├── packages/                         # 共享包工作区
│   ├── shared/                       # 共享类型与常量定义
│   ├── tsconfig/                     # TypeScript 配置集
│   └── eslint-config/                # ESLint 配置集
├── docs/                             # 文档目录
│   ├── plans/                        # 设计文档
│   └── project-management/            # 项目跟踪文档
└── package.json                      # 根 Monorepo 配置
```

### 3.2 主应用模块结构

markdown-editor 应用内部采用清晰的模块划分，主进程代码位于 src/main 目录，Preload 桥接层位于 src/preload 目录，渲染进程代码位于 src/renderer 目录。这种模块划分遵循了 Electron 应用的推荐架构，便于代码维护和功能扩展。

docs/reports 目录下存放项目的审查与测试报告，包括安全审查报告、代码审查报告和发布检查清单等重要文档。这些报告记录了项目开发过程中的各项检查结果和质量保证措施。

### 3.3 共享包结构

packages 目录下包含三个共享包，分别承担不同的职责。shared 包提供跨应用共享的类型定义、常量声明和工具函数，确保不同模块之间的类型一致性。tsconfig 包统一管理 TypeScript 编译配置，所有应用共享相同的编译规则。eslint-config 包定义代码规范检查规则，帮助团队保持统一的代码风格。

## 四、主要模块职责

### 4.1 主进程模块

主进程模块是 Electron 应用的运行核心，负责管理系统级资源和处理底层操作。该模块的主要职责包括窗口创建与管理、应用生命周期控制、文件系统操作代理、系统对话框调用以及 IPC 通信处理。

主进程通过 BrowserWindow API 创建和管理应用窗口，配置窗口大小、标题栏样式、菜单栏等属性。在窗口管理方面，主进程需要处理窗口的显示、隐藏、最小化、最大化、关闭等事件，并维护多个窗口之间的状态同步。

文件系统操作是主进程的重要职责之一。由于渲染进程运行在沙箱环境中，无法直接访问文件系统，因此所有文件读写操作都需要通过主进程代理完成。主进程提供文件打开对话框、保存对话框的实现，以及实际的文件读取和写入操作。此外，主进程还需要处理文件编码的自动检测，支持包括 GBK、Shift-JIS 在内的多种字符编码格式。

### 4.2 Preload 桥接模块

Preload 桥接模块在 Electron 应用中扮演着安全守门人的角色，它在渲染进程加载之前执行，通过 contextBridge API 向渲染进程暴露有限的、安全的 API 接口。这种设计确保了渲染进程无法直接访问 Node.js API 和 Electron 主进程模块，同时仍能通过受控接口完成必要的系统操作。

Preload 模块需要定义清晰、安全的 API 接口，通常采用命名空间组织相关功能。例如，文件系统相关接口可以命名为 window.electronAPI.files，操作窗口相关的接口命名为 window.electronAPI.window 等。每个接口都应该明确其参数类型和返回值类型，便于渲染进程调用和类型检查。

该模块还负责在主进程和渲染进程之间建立 IPC 通信通道。通过 ipcRenderer.invoke 方法，渲染进程可以向主进程发送异步请求，主进程通过 ipcMain.handle 注册处理函数进行响应。这种基于 Promise 的通信模式使得异步操作的处理更加便捷和直观。

### 4.3 渲染进程模块

渲染进程模块是用户界面的构建者，运行在 Chromium 沙箱环境中。该模块使用 React 框架构建组件化的用户界面，主要职责包括编辑器功能实现、预览渲染、文件树管理、工具栏交互和主题切换等。

编辑器功能是渲染进程的核心功能之一。项目采用 CodeMirror 6 作为编辑器内核，这是一个高度可定制的代码编辑器组件。CodeMirror 提供了强大的文本编辑能力，包括语法高亮、代码折叠、自动补全等功能。通过配置 CodeMirror 的扩展和主题，可以实现 Markdown 语法的实时高亮显示。

预览渲染功能将 Markdown 文本转换为 HTML 并进行展示。项目使用 unified 生态系统处理 Markdown，包含 remark 和 rehype 两个主要处理链路。remark 负责解析 Markdown 语法树，rehype 负责生成 HTML。通过这两个处理器的组合，可以实现 Markdown 到 HTML 的完整转换。为了支持富文本内容，项目集成了 KaTeX 用于数学公式渲染、Mermaid 用于图表渲染、highlight.js 用于代码高亮。

文件树管理功能允许用户在编辑器中浏览和选择本地文件系统中的文件。该功能需要与主进程协作，通过 IPC 通信获取目录结构信息，并在界面上以树形结构展示。文件树还支持文件的展开和折叠、文件的选中和高亮等交互操作。

工具栏交互提供了一组便捷的操作按钮，用于快速插入 Markdown 语法。工具栏按钮覆盖了常见的格式操作，包括加粗、斜体、标题、列表、引用、链接、图片等。每个按钮对应一个快捷键，用户可以通过键盘快捷键快速完成格式化操作。

主题切换功能允许用户在明暗两种主题之间切换。项目使用 CSS 变量驱动主题系统，所有颜色和样式都定义为 CSS 变量，切换主题时只需要更改根元素的变量值即可实现全局样式更新。

## 五、关键功能说明

### 5.1 所见即所得编辑

本编辑器实现了真正的所见即所得编辑体验，采用双栏布局同时展示编辑区和预览区。用户可以在左侧编辑区输入 Markdown 文本，右侧预览区实时显示渲染后的效果。为了保证流畅的用户体验，系统采用 300ms 自适应防抖技术，避免频繁的重新渲染操作。

预览区使用 unified 处理链将 Markdown 转换为 HTML，并通过 rehype-sanitize 进行安全过滤，防止恶意代码注入。转换后的 HTML 通过 DOMPurify 进一步净化，确保最终渲染的 HTML 符合安全标准。

### 5.2 预览面板开关

编辑器支持预览面板的打开和关闭功能，用户可以根据需要切换预览区的显示状态。当预览面板关闭时，编辑区自动扩展占满整个窗口，提供更大的编辑空间；当预览面板重新打开时，恢复之前的双栏布局。

预览面板开关可以通过工具栏按钮或快捷键 Ctrl+Shift+P 触发。切换操作带有平滑的过渡动画，确保用户体验流畅。预览面板的显示状态会持久化保存，应用重启后自动恢复上次的设置。

### 5.3 文件管理功能

文件管理功能提供了完整的本地文件操作能力。用户可以通过打开对话框选择本地文件，也可以通过目录树浏览选择文件夹并查看其中的文件列表。文件打开后，内容将加载到编辑器中，用户可以开始编辑工作。

文件保存功能支持手动保存和自动保存两种模式。手动保存通过 Ctrl+S 快捷键触发，弹出保存对话框让用户选择保存位置和文件名。自动保存功能在检测到内容变化后一段时间自动保存文件，防止意外丢失编辑内容。

编码自动检测是文件管理功能的重要特性。系统使用 jschardet 库自动检测文件编码，支持包括 UTF-8、GBK、Shift-JIS、ISO-8859-1 在内的多种常见编码格式。这种自动检测能力确保了编辑器能够正确打开各种来源的文本文件，避免出现乱码问题。

### 5.4 工具栏与快捷键

工具栏提供了一组可视化按钮，用于快速执行常用操作。按钮按照功能分组排列，包括文本格式化组、插入元素组、编辑操作组等。每个按钮都有对应的工具提示，鼠标悬停时显示功能说明和快捷键提示。

项目定义了 18 个以上的快捷键，覆盖了各种编辑操作。主要快捷键包括：Ctrl+B 用于加粗选中文本、Ctrl+I 用于斜体化、Ctrl+K 用于插入链接、Ctrl+Shift+K 用于插入代码块、Ctrl+Shift+P 用于切换预览面板等。标题级别的快捷键为 Ctrl+1 至 Ctrl+6，分别对应一级到六级标题。列表操作包括 Ctrl+Shift+U 无序列表、Ctrl+Shift+O 有序列表、Ctrl+Shift+] 引用块等。

### 5.5 富文本渲染

除基本的 Markdown 语法外，编辑器还支持多种扩展的富文本内容渲染。数学公式渲染使用 KaTeX 库，支持行内公式和块级公式两种形式。行内公式使用单个美元符号包裹，如 $E=mc^2$；块级公式使用双美元符号包裹，渲染为独立的段落。

图表渲染使用 Mermaid 库，支持流程图、时序图、甘特图等多种图表类型。Mermaid 图表代码使用三个反引号包裹，并指定语言为 mermaid。Mermaid 在渲染时会解析图表代码并生成对应的 SVG 图形，确保图表与文字内容无缝融合。

代码高亮使用 highlight.js 库，支持包括 JavaScript、Python、Java、C++、Go、Rust 在内的 10 种以上编程语言。代码块渲染时自动检测语言并应用对应的语法高亮主题，使代码内容易于阅读和理解。

### 5.6 自动目录生成

侧边栏目录功能自动分析文档中的标题结构，生成可导航的目录树。目录树以缩进表示标题层级关系，一级标题为顶级节点，二级标题为其子节点，以此类推。用户点击目录项，光标会自动跳转到对应标题位置，同时编辑器会滚动到该标题可见。

目录还与编辑器光标位置同步高亮。当用户在编辑器中滚动或移动光标时，目录树会自动更新高亮状态，显示当前光标所在的标题节点。这种同步机制帮助用户快速了解文档结构定位。

### 5.7 滚动同步

编辑区和预览区支持双向滚动同步。当用户在编辑区滚动时，预览区会自动滚动到与编辑位置对应的内容区域；反之亦然。滚动同步采用段落级比例同步算法，根据当前可见段落的内容位置计算预览区的目标滚动位置，确保两个区域始终保持视觉对齐。

### 5.8 主题系统

项目实现了完整的明暗双主题系统。用户可以通过设置界面或快捷键切换主题。主题切换通过 CSS 变量实现，所有颜色、背景、边框等视觉属性都定义为 CSS 变量，存放在主题定义文件中。切换主题时，只需更改根元素上的主题类名，即可触发全局样式更新。

主题定义遵循一致性原则，定义了文本颜色、背景颜色、边框颜色、悬停状态、激活状态等完整的视觉规范。编辑器语法高亮颜色也会随主题变化，确保代码在不同主题下都有良好的可读性。

### 5.9 导出功能

编辑器支持将 Markdown 文档导出为 HTML 和 PDF 两种格式。导出 HTML 时，系统会将 Markdown 内容转换为完整的 HTML 页面，包含内联样式，确保导出的文件在任何浏览器中都能正确显示。数学公式、图表和代码高亮等内容会保留其渲染后的样式。

导出 PDF 时，系统首先将 Markdown 转换为 HTML，然后通过 Electron 的打印功能生成 PDF 文件。PDF 导出支持页面布局配置，包括页边距、页面方向等选项。导出的 PDF 文件保留文档的完整格式，包括图片、图表和数学公式。

### 5.10 安全机制

项目采用多层安全防护机制确保应用安全。首先，内容安全策略（CSP）限制页面可以加载的资源来源，防止跨站脚本攻击。其次，Markdown 渲染使用 rehype-sanitize 对 HTML 进行净化处理，移除潜在的恶意代码。再次，编辑器内容通过 DOMPurify 二次净化，确保最终显示的内容安全无害。

Electron 层面的安全措施包括：启用 contextIsolation 选项将 Preload 脚本与页面隔离；启用 sandbox 选项限制渲染进程的权限；禁用 nodeIntegration 防止页面直接访问 Node.js API；使用 contextBridge 安全地暴露有限的 API。这些措施共同构建了纵深防御体系。

## 六、依赖关系

### 6.1 应用层依赖

markdown-editor 应用直接依赖多个核心库实现其功能。这些依赖按照功能可分为几个主要类别：

编辑器核心依赖包括 CodeMirror 6 及其扩展包。CodeMirror 提供基础的文本编辑能力，其扩展包包括 @codemirror/lang-markdown 用于 Markdown 语法支持、@codemirror/theme-one-dark 提供深色主题等。这些包共同构成完整的编辑器功能。

Markdown 处理依赖包括 unified 核心库、remark-parse、remark-rehype、rehype-stringify 等。remark 系列包负责 Markdown 解析，rehype 系列包负责 HTML 生成。此外还有 rehype-sanitize 用于内容净化、rehype-katex 用于数学公式渲染等扩展插件。

UI 组件依赖包括 React 及其相关库用于构建用户界面。状态管理使用 Zustand 库，这是一个轻量级的 React 状态管理方案，比 Redux 更加简洁。样式管理使用 Tailwind CSS 或 CSS Modules。

### 6.2 共享包依赖

shared 包作为类型定义的集中管理点，通常不依赖其他外部包，主要为其他模块提供类型服务。tsconfig 包包含 TypeScript 编译器配置，定义了 strict 模式、目标平台、模块解析策略等编译选项。eslint-config 包依赖 ESLint 核心库和各类插件规则。

### 6.3 间接依赖

项目通过 pnpm 的 workspace 机制管理依赖，所有应用共享顶层的 node_modules。当某个包被多个应用引用时，pnpm 会在顶层安装一份副本，避免重复安装。这种设计既节省磁盘空间，又加快了依赖安装速度。

## 七、运行方式

### 7.1 环境准备

在运行项目之前，需要确保开发环境满足以下要求：Node.js 版本建议为 LTS 版本；pnpm 包管理器需要安装且版本不低于 9.15；操作系统需要支持 Electron 运行的桌面环境（Windows、macOS、Linux）。

安装依赖前，需要在项目根目录执行 pnpm install 命令。pnpm 会自动分析 workspace 配置，安装所有子包的依赖项。由于项目使用 pnpm workspace 机制，所有依赖会尽可能在根目录共享安装，减少磁盘占用和安装时间。

### 7.2 开发模式

开发模式下运行应用使用 pnpm dev 命令。该命令会启动 Vite 开发服务器和 Electron 主进程，支持热模块替换（HMR）功能。在开发模式下，代码修改可以实时生效，无需手动重启应用。

开发服务器默认监听某个本地端口，Electron 主进程会加载该端口的页面进行预览。开发者工具默认开启，便于调试和检查页面状态。Vite 的快速热更新特性确保了编辑体验的流畅性。

### 7.3 类型检查

代码质量保证通过 pnpm typecheck 命令执行。该命令运行 TypeScript 编译器检查所有代码的类型正确性。项目启用 strict 模式，对类型检查有严格的要求，任何类型不匹配或隐式 any 类型都会导致编译错误。

类型检查通常在提交代码前和 CI/CD 流程中执行，确保代码库的 type-safety。开发过程中建议开启编辑器的 TypeScript 支持，实时获取类型错误提示。

### 7.4 代码检查

代码风格检查通过 pnpm lint 命令执行。ESLint 会根据配置的规则扫描代码，报告不符合规范的代码模式。检查范围包括代码格式、命名规范、潜在错误、最佳实践等方面。

项目配置了合理的 ESLint 规则，既保证代码质量又不过度限制。开发者应修复所有 lint 错误后再提交代码，或者通过配置注释临时忽略特定行。

### 7.5 生产构建

生产构建使用 pnpm build 命令执行。构建过程会进行代码压缩、优化和打包，生成可在生产环境运行的产物。Electron 应用的构建通常包括两个阶段：首先构建渲染进程的 Web 资源，然后使用 electron-builder 打包为可执行文件。

构建完成后，产物通常位于应用目录的 dist 或 release 目录下。不同平台的产物格式不同：Windows 平台生成 .exe 安装包，macOS 平台生成 .dmg 磁盘镜像，Linux 平台生成 .AppImage 或 .deb 包。

## 八、开发规范

### 8.1 代码风格

项目使用 ESLint 和 Prettier 协同维护代码风格。ESLint 负责检查代码逻辑问题和潜在错误，Prettier 负责格式化代码风格。所有代码应符合配置的规范，在提交前通过 lint 检查。

TypeScript 代码应充分利用类型系统，使用明确的类型注解避免使用 any 类型。函数参数和返回值应声明类型，对象接口应明确定义结构。这种严格的类型规范有助于提高代码的可维护性和重构安全性。

### 8.2 Git 提交规范

提交信息应遵循约定的格式，清晰描述本次提交的内容。建议使用 feat、fix、docs、style、refactor、test、chore 等标签开头，后跟冒号和具体描述。例如：feat: 添加文件保存功能、fix: 修复滚动同步失效问题。

提交前应确保代码通过所有检查，包括类型检查、lint 检查和测试。避免提交包含 TODO 注释或调试代码的提交，保持代码库的整洁。

### 8.3 模块组织

代码模块应遵循单一职责原则，每个模块专注于特定功能。模块之间通过明确定义的接口通信，避免直接依赖内部实现细节。这种设计使得模块可以独立开发、测试和维护。

React 组件应采用函数式组件和 Hooks 的写法，充分利用 React 19 的新特性。组件文件应与样式文件分离管理，或使用 CSS-in-JS 方案。组件应提供清晰的 props 接口定义，便于使用和维护。

### 8.4 安全注意事项

处理用户输入时应始终进行验证和净化，防止注入攻击。文件系统操作应在主进程完成，避免将文件路径直接暴露给渲染进程。IPC 通信应验证消息来源，防止恶意页面的非法请求。

敏感信息如 API 密钥、用户凭证等不应硬编码在代码中，应使用环境变量或安全的存储方案。CSP 配置应尽可能严格，只允许加载必要的资源。

## 九、已知限制与未来计划

### 9.1 当前缺口

根据项目文档，当前版本存在一个已知缺口：文件树右键菜单功能尚未实现。该功能包括在文件树中右键点击文件或文件夹时弹出上下文菜单，提供新建文件、新建文件夹、重命名、删除等操作选项。该功能将在下一迭代周期中实现。

### 9.2 后续规划

项目的后续开发将聚焦于以下方向：完善文件树右键菜单功能；支持更多文件编码的自动检测；增强主题系统的自定义能力；优化大型文档的编辑性能；添加更多编辑器扩展功能。

## 十、相关资源

### 10.1 项目文档

项目的主要文档位于 docs 目录下，包括设计文档和项目跟踪文档。详细的功能设计和技术方案可参考 docs/plans 目录下的设计文档。项目进度和任务跟踪信息可参考 docs/project-management 目录下的管理文档。

### 10.2 审查报告

项目在开发过程中进行了多项审查工作，相关报告位于 apps/markdown-editor/docs/reports 目录。安全审查报告（security-review-p4.17.md）详细分析了项目的安全架构和潜在风险。代码审查报告（code-review-p4.18.md）提供了代码质量评估和改进建议。发布检查清单（release-checklist.md）列出了发布前需要确认的事项。

### 10.3 快速参考

以下命令是日常开发中最常用的快捷命令：

```bash
# 安装依赖
pnpm install

# 启动开发环境
pnpm dev

# 类型检查
pnpm typecheck

# 代码检查
pnpm lint

# 生产构建
pnpm build
```

项目详细信息请参阅根目录的 README.md 文件和应用目录下的 README.md 文件。
